#include <cs50.h>
#include <stdio.h>

int main (void)
{
int n;
do{
    n = get_int("Height: ");
}
while (n < 1 || n > 8);

for(int i = 0; i < n ;i++)
{
    for(int x =0; x < n; x++)
    {
        if (i+x < n-1)
        printf(" ");
        else
        printf("#");
    }
    printf("\n");
}
}
